# Copyable independent workplace validation and rollout prompt


Current validation prompt for the feature checkpoint `519f8ca` and subsequent handover cleanup. Start with [workplace handover](../WORKPLACE_AZURE_HANDOFF.md) and [current status](../STATUS.md). Record the actual revision and rerun checks; historical test counts are not current acceptance.

Review this cloud-governance repository's actual Azure Functions implementation before workplace rollout. Read docs/AZURE_FUNCTIONS.md and inspect function_app.py, hosting.py, azure_adapters.py, workflow.py, provenance.py, archive.py, pdf_report.py, host.json, requirements/constraints, settings templates and packaging scripts. Treat previous Linux fixture results as offline evidence only; no Azure or macOS validation is implied. Do not change unrelated work or trust a previous completion claim without checking the current revision.

The required architecture is macOS or Linux development/testing and production in Azure Functions without a workstation runtime. CollectEvidence is scheduled only with an explicit workplace schedule and enable flag; GenerateReport is a separate key-protected POST that requires one exact saved run ID. Verify there is no anonymous route, implicit latest selection, automatic PDF requirement or runtime AI. Confirm production cannot use Azure CLI/default credential fallback and explicitly selects the assigned managed identity. Verify tenant and subscription checks use supported authenticated mechanisms, never unverified JWT decoding. Confirm report generation cannot call collection, assessment or current context to rewrite historical meaning.

Run the complete offline gate with the pinned developer requirements: zero unexpected skips, existing collector/rule tests, managed-identity/configuration tests, real-SDK request-condition tests, Blob adapter failures/pagination/concurrency and hosted orchestration. Check conditional If-None-Match protection for single uploads and multipart final commits. Test unrelated identical-byte collisions, same-attempt lost acknowledgements, mismatched bytes/metadata, denied/missing/partial reads, bounded retries, malformed key/prefix/endpoint rejection, every publication failure stage and failed failure-marker writes. No partial publication may be presented as complete; failure markers override committed manifests, and missing markers after lost acknowledgements require reconciliation rather than fabricated certainty.

Demonstrate multiple saved runs and distinct report versions using synthetic data. Select an older exact run while preventing ARM calls, collection, assessment and refreshed context. Compare all saved facts/results/context bytes and earlier PDF bytes before/after. Verify hashes, versions, original times, new generation time, execution/backend provenance and missing-provenance behavior for older archives. Extract PDF content and check embedded fonts/internal evidence links; visually review affected pages if the renderer changed. Auditors must receive one self-contained PDF without database, JSON, private Blob URL or external spreadsheet dependence. Exercise the multi-source rehearsal and full/topic/family/control report selections, including POST /api/evidence/reports under CG_REPORT_ENABLED. Confirm positive, adverse and incomplete evidence stays visible, exact Wiz/operating attachments are preserved, and publication succeeds independently of adverse findings. The implementation includes 199 configuration predicates and project-defined Wiz v1/v2 imports; a native Wiz connector and combined guest/Kubernetes selected-report rows remain unfinished. All 215 research objectives remain open/partial across the 23-service/20-family program; evidence is not control satisfaction.

Verify a fresh wheel installation from outside the checkout and the allowlisted Functions source package. Confirm packaged program_scope.json, control_objectives.json, control_index.json, evidence_report.py, wiz_observations.py and ReportLab fonts, Linux dependency build, trigger indexing, host settings and development/deployment exclude rules. Run on the intended work Mac and approved Linux runtime; do not substitute one OS result for another. Check all tracked files/history for credentials and real evidence without printing secrets. Review GitHub Actions permissions/cost policy before enabling runs; CI configuration alone is not evidence of a run.

Before live validation, require actual approved tenant/identity assignment, explicit subscriptions, Function App plan/region, separate runtime/evidence storage, existing evidence container/prefix, role scopes, network/private endpoint/DNS path, report caller authorization, UTC schedule, retry/overlap/time budgets, package policy, classification/recipients, retention/legal holds, logging/recovery owners and test/cost authorization. Verify runtime storage can perform coordination writes and is not under evidence retention locks. Review managed-identity ARM permissions independently from evidence Blob permissions. Test work-tenant caller authentication and network restrictions as well as absent/wrong function keys; local Core Tools key enforcement is disabled and cannot prove production security.

Only with explicit live authorization, run bounded checks against the specified existing nonproduction workplace resources. Verify tenant mismatch rejection, access denials, provider APIs, scheduler coordination, safe pre-archive failures, completed FAIL/INCOMPLETE findings, no overwrites, manifest completion and exact-run PDF generation. Measure collection duration, peak memory and synchronous PDF latency with realistic scope; cooperative budgets do not hard-cancel in-flight work and the HTTP/platform time limits still apply. Review duplicate/redelivery and concurrent-worker behavior; UUID uniqueness is not exactly-once scheduling. Do not add an async platform or alter scope to hide a failed sizing gate.

Do not create resources, push source, grant roles/consent, enable paid services, deploy, upload real evidence, change network access, delete protected archives or lock retention solely because this prompt exists. Honor separately supplied current authorization for exact actions. Keep unresolved inputs explicit. Produce a concise readiness record separating offline, macOS, Linux package and live Azure results, actionable findings, rollback/disable steps and remaining workplace decisions. Software readiness is not corporate compliance certification.

Use the user's existing workplace infrastructure patterns and docs/WORKPLACE_RUNTIME_CONTRACT.md. infra/personal-lab is for home integration tests only; do not deploy it at work or obtain/replace the user's work Terraform. The public v0.3.0 commit 83b0711 passed Linux/macOS CI (105 tests each, no skips) at https://github.com/csGIT34/auditevidencecollector/actions/runs/35452053904; later local changes need their own verification. Optional CG_RESOURCE_GROUP restricts one explicit subscription to one RG, and CG_EXECUTION_EXPIRES_AT bounds the start of temporary test operations. Never include the optional lab probe in a production package.

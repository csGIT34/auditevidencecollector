# Local preparation validation (0.3.1)

2026-09-19, Linux x86_64 Python 3.12 / Terraform 1.15.9. Terraform is **home-only**. No cloud infrastructure, role, app deployment, test evidence upload, notification or provider registration was performed. No Azure-connected Terraform plan, apply or destroy was run.

- Application gate: **117 tests passed, zero skips**, including RG-only inventory/hydration, rejection of out-of-group rows and changed pagination, denied scope with no fallback, safe reuse across scopes, saved scoped-run/PDF roundtrip, old snapshot compatibility, expired operations and synthetic probe no-overwrite/history. The existing encryption/archive/SDK tests remain in the gate.
- Research catalog/packaged outline validated unchanged: 23 services, 20 families and 215 proposed checks. They remain research, not additional live collectors.
- Terraform: pinned official providers initialized; `fmt -check`, `validate` and **four mocked, plan-only tests** passed. Tests cover disabled operations/no schedule, two LRS/Hot Entra-only accounts, no always-ready, bounded scale, RG scope, narrow metadata/evidence roles, /32 ingress, rejected broad ingress/unreviewed region and expired lab. Provider checksums cover Linux amd64 and both Mac architectures. Mock testing does not validate Azure policy, actual role propagation, API runtime or capacity.
- The builder rejects ARM/Linux before installing or writing, covered by a targeted regression. The allowlisted Linux dependency build used the existing pinned wheelhouse without a package-network request. The optional lab ZIP is about **16.2 MB** and includes pinned runtime dependencies and the disabled lab-only probe. Clean extraction/import using only packaged dependencies confirmed version 0.3.1, package data, two FUNCTION-key-protected SDK definitions and disabled probe. This is not a Functions host startup. Production source packaging excludes all Terraform and the probe.
- Personal preflight was read-only and bounded. Raw personal Azure metadata/billing responses are outside the repository, not in this public record. Actual billing headroom, forecast, policy costs and subscription-specific Flex quota remain apply gates when unresolved. The generic cost model is [COST.md](COST.md), not a personal bill report.
- Source review checks personal IDs, secrets, Terraform state/plans/real tfvars, evidence, outputs and dependency directories before a local commit. No public push is included in this preparation task.

Published baseline `83b0711` passed [Linux and macOS GitHub CI](https://github.com/csGIT34/auditevidencecollector/actions/runs/35452053904) with 105 tests per OS, zero skips and package builds. That result applies to the baseline only; this newer local slice has not been pushed or run in GitHub/macOS CI. The user's actual work Mac and live workplace network/caller controls still require acceptance using existing workplace infrastructure patterns.

Remaining live gates: billing-period/currency/headroom confirmation under the total-$25 condition; inheritance/diagnostic export costs; region/Flex quota/provider registration; approved operator /32/object ID and unique lab names; a reviewed private create plan; explicit creation/deployment/test authorization, then the already-authorized project-only cleanup after successful workplace handoff, artifact preservation and scope verification. Follow [README.md](README.md); do not provision solely because local validation passed.

This is the historical preparation record. Later live observations and v0.3.2 fixes are recorded separately in [LIVE_VALIDATION.md](LIVE_VALIDATION.md).
